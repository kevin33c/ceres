/*
const assert = require('assert');

class Car {
    park() {
        return 'stopped';
    }

    drive() {
        return 'vroom';
    }
}

//CHANGE package.json test: mocha

//set up global variable car
let car;

//before each IT test setup
beforeEach(() => {
    car = new Car();
})

//group of its
describe('Car', () => {
    //individual tests
    it('can park', () => {
        assert.equal(car.park(), 'stopped');
    });

    it('can drive', () => {
        assert.equal(car.drive(), 'vroom');
    });
});
*/